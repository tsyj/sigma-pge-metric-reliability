# E56_VERDICT.json 的 A1 口径勘误

**矛盾**：`e56/E56_VERDICT.json` 同时存在 `paired_A1 = 1.0` 与 `A1_paired_correct = 0.0`。

**查明**（数字由 `e71_innov/repo_fixes/scripts/fix_03_e56_a1.py` 从 `e56/E56_BH93.json` 现算，
核算件 `E56_A1_RECOMPUTE.json`）：

- BH93 静止态 7 档 VISC2 × {flat, r26steep} 配对；平底 `u_max` 全部精确为 0，海山 `u_max` 全部 > 0。
- A1（配对抗糊弄）按 E40 口径 = **平底读数 ≥ 海山读数的配对比例**（换到零真值平底，读数不许更好看）：
  0/7 = **0.000** —— 即该尺子换到平底读数精确归零、看起来完美，**完全不抗刷**。
- `paired_A1 = 1.0` 是早期计算把比较方向写反（算成了"海山读数 ≥ 平底读数的比例" = 7/7 = 1.0）的**废弃值**。

**判定以 `A1_paired_correct = 0.000` 为准**。仓库内所有下游引用（`scripts/reproduce_core.sh` 3/4 段、
附录D §4.6、kill board A9 相关表述）均已使用 0.000 口径；`paired_A1` 键按"封存产物不改"原则原样保留，
仅在此声明废弃。

生成：2026-09-17
