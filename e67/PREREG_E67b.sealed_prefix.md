# E67b 预注册：配方能不能跨强迫方向搬运（修正判据错配后重做）

**登记时间**：2026-09-05 凌晨，在 E67 v1 作废之后、本实验运行之前。
**与 v1 的唯一差别**：换向后每个集合用**选它的那把判据**判读，而不是统一用原口径 A。
- S_A　在新方向上：A ≥ 0.7 且 B ≥ 0.9
- S_A⊥ 在新方向上：**A⊥ ≥ 0.7** 且 B ≥ 0.9（A⊥ 在新方向上重新计算）
A⊥ 是更严的判据（全体 P(A⊥≥0.7)=0.094 vs P(A≥0.7)=0.316），因此这不是放宽。

## 预测（重新登记，不沿用 v1）
- **Q1**：S_A⊥ 的跨向存活率 > S_A 的跨向存活率。（配方的核心主张）
- **Q2**：两者相对各自底率的富集都 ≥ 2 倍。
- **Q3**：S_A⊥ 的三向交集 > 0。（v1 报的 0 已作废）
- **Q4**：跨向存活者全部是比值型。

## 判读
任何一条被推翻都如实记录。**若 Q1 再次被推翻，说明「扣掉伪流代理」这一步
并不能改善跨场景可迁移性，必须原样报出，不得再改判据重试**——
改判据重试本身就是本作品所批判的那种行为。
