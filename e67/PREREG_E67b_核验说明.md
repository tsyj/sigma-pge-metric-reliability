# PREREG_E67b.md 的哈希核验说明（2026-09-16 补记）

## 发生了什么
2026-09-05 00:12:30 登记预注册（`PREREG_E67b.sha256` 记录 `9ee57f04…`，`PREREG_E67b.stamp` 记录时间），
00:12:51 实验运行完成（`E67B_RECIPE_TRANSFER.json` 与 `e67b.log`）。
00:14:54 把"结果与如实记录"**追加到了同一个文件末尾**（以 `---` 分隔，标题注明"实验完成后追加，预注册正文未改"）。
因为 sha256 是对整个文件计算的，追加之后全文哈希变为 `d06c5787…`，与登记值不再相等。

这是流程错误：结果应当另存文件，不应追加进预注册文件。

## 如何证明预测原文未被改动
登记的是**文件前 1136 字节**。只取这一段重新计算，哈希与登记值逐位相等：

    head -c 1136 PREREG_E67b.md | sha256sum
    cat PREREG_E67b.sha256

两行输出的哈希相同 ⇒ Q1–Q4 四条预测及判读规则在运行前后逐字节一致；
第 1137 字节起是运行后追加的结果记录，不属于预注册内容。

另一条独立旁证：`e67b_recipe_transfer.py`（00:12:30 写入，早于运行）第 107–111 行
硬编码的 Q1–Q4 判读条件，与预注册正文的四条预测一致。

## 处置
原文件保持原样不动（保留证据）。今后预注册文件登记后只读，结果一律另存 `RESULT_*.md`。

## 补记（2026-09-17）：追加段里的「45° 只剩 5 把」不是两关幸存数
追加段第 35 行（字节偏移 1837，0 起算；封存段为前 1136 字节）「45° 只剩 5 把」指 45° 全场 15376 把中满足 A⊥≥0.7 且 B≥0.9 的把数（与 `diag45.base_rate_perp`=0.0003 × 15376 ≈ 4.6 取整一致；`e67b.log` 与 JSON 均未存精确计数，按该四舍五入到 4 位的基率只能确定为 4 或 5），不是两关幸存数；纬向 444 把 S_A⊥ 搬到新方向后实测存活为 **0**（`E67B_RECIPE_TRANSFER.json`: `merid.S_AP.n`=0、`diag45.S_AP.n`=0、`triple.S_AP`=0）。材料口径「45° 两关幸存 0 把」以此为准。
