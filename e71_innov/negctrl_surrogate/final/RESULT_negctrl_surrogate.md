# RESULT：negctrl_surrogate 验证批（E72）——步骤 3 完成报告

> 【2026-09-17 复算更正】 本文件已按战报§5.1–5.3和既有复算收紧口径；所有原判分JSON与封存件保留。文末列逐项改动清单，历史数不得脱离更正引用。

执行：2026-09-17（封存 14:55:35+08:00，首读留出 14:55:45，判分 14:57:21；时间按复算stat更正）
目录：`/data/xinyuan/GOAI_ai4s_env/e71_innov/negctrl_surrogate/final/`

---

## 1. 检查意见落实（全部阻断与重要意见，封存前完成）

| 意见 | 处理 | 验证 |
|---|---|---|
| [阻断] seal.sh grep 命中 PREREG 第 6 行说明文字 | 双保险：seal.sh 改 `grep -q '^状态：草稿'`（行首锚定）＋ PREREG 第 6 行改写为不含该子串 | 封存一次成功，无计划外二次编辑 |
| [重要] P5 两子句答案近乎在盘（sweep 账本 obs 自带 deep_rms_800/u_max） | **P5 降级为验证性断言 V4**（PREREG §1.4），真预测缩为 P1–P4、P6、P7 共 6 条；§1.2 补披露行（纬向核验 obs 字段：u_max 14/14 逐位同、deep_rms_800 3/14 逐位同其余 ≤1e-2）；判分器照算照印于 `verdict.json /descriptive_P5_demo`，不计数 | verdict.json `n_predictions=6`；obs 字段核验只开了纬向 knob_sweep.json，未开留出账本 |
| [重要] §2「零读取」在文件 IO 层面不成立（regraded_v2 整文件载入过） | §2 语义声明改写为如实申报（曾整文件载入、仅按纬向 run_id 索引、留出行数值未输出未落盘未参与决策，不主张 IO 层零读取）；加固：domain_block 现先载本域账本 run_id 集再**按域过滤**载入 regraded_v2 | 自检重跑 **58/58 全绿**（数值与整文件载入等价，46s） |
| [重要] 封存门只核 2/5 项哈希，脚本可封存后偷改 | 两脚本 require_seal 重写：核对入档**全部五件**；PREREG/SEEDS82 不符**永不放行**，两脚本与自检文件不符仅当 DEVIATIONS.md 在场才放行；运行时脚本 sha 印入 E72_VALID.json 与 verdict.json | 假 .sha256 三场景实测全部拒跑/拒判（脚本哈希错、PREREG 哈希错、自检哈希错），假档随即删除；正式跑批中 runtime sha == sealed sha |
| [一般] p6() 缺 pairs_ok 门 | p6 开头加与 P1–P4 相同的 pairs_ok AMBIGUOUS 分支；PREREG §3-7 同步写明 | py_compile 通过；本批两域 28 对未触发 |
| [一般] domain_block 崩溃会砸掉整个 --holdout | holdout() 对每域 try/except：异常域落 `{error, traceback, status:AMBIGUOUS}` 块并继续另一域；PREREG §3-7 补域级崩溃条款 | py_compile 通过；本批两域均正常未触发 |

封存前时序合规：全部修正 → 自检重跑（只读纬向）→ 负门实测（无封存拒跑）→ 封存 → 才读留出。**留出读数在封存前零输出、零使用**（IO 层历史接触已在 §2 如实申报）。

## 2. 封存记录

- PREREG sha256：`c64f1390037f9dce4a0be13a4b0179ed26d9919285a1e9897e4b445f3695a611`
- e72_valid.py：`5b0919b7…`；e72_verdict.py：`7c482d7e…`；SEEDS82.json：`561dc4ea…`（与验证前存档一致）；E72_SELFTEST_ZONAL.json：`7b593f1b…`
- 封存时间：`2026-09-17T14:55:35+08:00`；chmod 444；五件全部入 `.sha256`

## 3. 判分结果（verdict.json，机械输出；真预测 6 条：4 TRUE / 2 FALSE / 0 AMBIGUOUS）

| 预测 | 判定 | merid | diag45 |
|---|---|---|---|
| P1 结构复现 ρ(D\*logr,A\*logr)≥0.5 | **TRUE** | 0.8012（p<0.001（2000次零次超过；纬向标签口径） 【2026-09-17 复算更正】） | 0.8711（同；仅22对本域真值为0.990946，超出点预测区间） 【2026-09-17 复算更正】 |
| P2 82 把存活≥30%∧富集≥5× | **冻结TRUE；本域真值22对FALSE、两域0/82** 【2026-09-17 复算更正】 | 74/82=90.2%，富集 6.16× | 36/82=43.9%，富集 8.70× |
| P3 ρ(A\*diff,B)≥−0.45 | **TRUE** | −0.3533（原始口径 −0.6127） | −0.3697（原始 −0.6168） |
| P4 max{A\*:|D\*|≤0.3}≤0.7 | **冻结FALSE；本域真值22对TRUE（0.357/0.423）** 【2026-09-17 复算更正】 | 0.7247 > 0.70（差 0.025） | 0.6612 ≤ 0.70 |
| P6 frac(|OS_lr|更接近1)≤0.55 | **FALSE**（推翻，原样保留） | 0.5643 > 0.55（差 0.014） | 0.4530 ≤ 0.55 |
| P7 τ绝对值≥0.85（已知量验证性） | **冻结TRUE，不作留出命中** 【2026-09-17 复算更正】 | 1.0（正对照描述性：判死·与旋钮共线） | 1.0（同） |

V4（原 P5，降级不计分，descriptive_P5_demo）：would_be TRUE——merid 5.66×/lr 6.877，diag45 5.71×/lr 4.757。

【2026-09-17 复算更正】 两域均有28对读数、15376候选、7档sweep；H按run_id借用了纬向标签，本域hidden只覆盖22/28对，缺6个AKV_BAK陡臂真值。

## 4. 结果读法（2026-09-17 复算更正）

- 冻结P2的74/82、36/82不能称两留出域真值结论；stable排序为73/82、34/82。本域真值22对的P2为两域0/82 FALSE；P4为0.357425/0.422925 TRUE，4T/2F总数不变但P2/P4互换。
- P1校正后相关冻结0.801/0.871，置换p<0.001（2000次零次超过）；本域真值22对为0.999513/0.990946，仍TRUE但不在点预测区间0.80–0.95。
- P6不用H，仍FALSE；经向0.5643仅比0.55高1.4个百分点，45°为0.4530、方向相反，无不确定度，不解读为止刷药的好消息。
- P7的τ=1.0已在纬向自测封存，降为验证性断言。V4仍描述性；约77倍只属纬向，经向约970、45°约116，不与62×互替。
- 真值版敏感性见 `../recompute/RC_PREDICT.json#/variants/own_skill|ms`。`RC_EXTRA.json`另含“保留纬向标签、仅筛22对”的对照，勿混用。

## 5. 执行备注（2026-09-17 复算更正）

- 已建DEVIATIONS.md，登记H标签来源、P7降格与不再重跑承诺。**此后不运行冻结批处理/判分**；不能利用DEVIATIONS存在时的哈希放行分支重跑。
- 首跑内存门受中文locale影响，原“人工三查”只有自述、无日志佐证；本轮计算由外层限内存，直接nice 19，8线程，不调用systemd-run。
- 文献口径为“本地库已查、外网待补”；已有API实查关键词不是本条两条杀检索，不冒称已经完成。
- BH93说明、外网杀检索仍待办；本轮仅更正已有材料，不补数据。

## 6. 产出清单（本目录）

- `PREREG_negctrl_surrogate.md`（444，已封存）＋ `.sha256`
- `E72_VALID.json`（留出统计，含 runtime 脚本 sha、elapsed 95.9s）
- `verdict.json`（机械判分，n_predictions=6）
- `E72_AXES_merid.npz`、`E72_AXES_diag45.npz`、`E72_BASEQ_merid.json`、`E72_BASEQ_diag45.json`
- `E72_SELFTEST_ZONAL.json`（新代码路径重跑，58/58）
- `SEEDS82.json`（sha 与验证前存档一致）、`seal.sh`、`run_valid.sh`（已修 locale）、`gen_seeds82.py`

## 2026-09-17 复算更正清单（当前引用口径）

本节旧说法仅为更正定位；以替代口径为准，不覆盖封存判分、不新增计算。

### 战报 §5.2 小错

| 条目 | 旧记录（已更正） | 当前替代口径 | 复算依据 |
|---|---|---|---|
| negctrl_surrogate P1 | perm p≈0.0005 | p<0.001（2000 次零次超过） | `recompute/RC_PREDICT.json` |
| negctrl_surrogate P2 的 X/Y | 74/82、36/82 | 随排序算法变：stable 排序为 73/82、34/82；且须先处理标签借用 | 同上 |
| negctrl_surrogate 时间 | 14:56 起读留出、判分 14:58 | 14:55:45、14:57:21 | 复算 stat |
| negctrl_surrogate 检索口径 | 据我们检索未见（本地 417 篇＋arXiv export API，2026-09-17） | 本地库已查、外网待补（已落盘的 API 实查是别的关键词） | 复算核对 |

### 战报 §5.3 说过头

| 战报序号 | 旧说法（撤回/收紧） | 当前替代口径 | 条目 |
|---|---|---|---|
| 15 | P2 通过版：82 把在两个从未读过的环境里活下来 74/82、36/82 | 【不可用】至少写"机械判 TRUE，但真值按 run_id 借用纬向；只用留出真值的 22 对为 0/82" | negctrl |
| 16 | P4 被推翻作诚信展品（不注） | 同注标签借用；用留出真值时 P4 通过 | negctrl |
| 17 | P6：经向校正略像家族级止刷药，是好消息 | 边际越线（0.5643 vs 0.55），单域，两域方向相反 | negctrl |
| 18 | P7 算留出预测押中 | 验证性断言 | negctrl |

详细偏差、敏感性指针与不重跑说明见同目录 `DEVIATIONS.md`。
