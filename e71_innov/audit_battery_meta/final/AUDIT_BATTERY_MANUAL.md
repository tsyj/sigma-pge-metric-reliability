# 审计电池说明书（一页）· A1–A9 能判什么、判不了什么

预注册 PREREG_audit_battery_meta.md（sha256 7b641565…79d9，封存 2026-09-17T17:16:04+08:00）；全部数字出自同目录 JSON，指针见 RESULT_audit_battery_meta.md。

## 第一页先写盲区（差分口径漏网，ABM_KILL_MATRIX.json:classes）
| 注入缺陷 | 漏网 | 为什么抓不住 |
|---|---|---|
| 单位错（×3.7817） | 5/5 | 全部可执行算子对正常数缩放不变——秩审计对单调重标度**按构造失明** |
| 归一化错（平方） | 5/5 | 平方保序；只可能经 A4′ 的幅变门槛触发，实测未触发 |
| 盒宽依赖（求和代替平均） | 4/4（另 1 个分子区域为 all 的等价体排除） | 同一网格上因子对全部陡臂是常数，不改序 |
| 相邻层错位 | 3/5 | 错一档的量与原量排序几乎相同 |

**抓住的**：符号翻转 5/5、深浅区域错置 5/5、无关分子 3/3、中位数截断 5/5（平均秩重跑同样 5/5，非并列破法伪迹）。
差分检出率 20/37 = 0.541（Wilson 95% [0.384, 0.690]）。只覆盖 8 类自建合成缺陷，不外推到一切刷分。
**绝对口径 37/37 = 1.00 不是能力**：5 把 Pareto 前沿基指标自身已在某算子判死，变异体全部"继承"死亡。

## 九把刀是什么（MR 四元组，mr_audit.yaml）
- 按构造零真值的 sound 证伪：**A1** 换平底、**A2** 零真值通道、**A9** BH93 静止态（判据同 A1/A8）。
- 启发式不变性：**A3** 盒宽、**A4** 时钟/相位、**A7** 换名。 等变性：**A6** 转风向。 单调性：**A8** 真值锚带符号 ρ。
- **A5 不是蜕变关系**，是程序性控制（预注册对照）。九项 ≠ 九份独立证据。
- A2 行正确写法：`|m(flat)| / |m(steep 典型)| < 1%`（MR_TAXONOMY.md 该行因竖线错列）。
- MT 不对称：违反即证伪、通过不认证。uv 纬向 32/32 的 Wilson CI [0.8928, 1] 跨 0.9 → 待定：**能判死，不能认证**。
- 对变异体电池：7 把可执行刀中 A2 只判绝对型，5 把基全为比值型 → **有效 6 把**。差分杀死次数 A6 15、A8 13、A9′ 8、A3′ 4、A1 3、A4′ 3（ABM_DERIVED.json:mutants）。

## 判定表自己也要挨查（ABM_MR_CONSISTENCY.json）
50 格中 22 格可按 RULES 从源 JSON 机械复导出，20 格一致、2 格不一致，原样公示、不改表：
temp_d400·A1（32/32 按三值化应"待定"，表记"过"）；deep_dc_rms·A6（经向 A=−0.99 过、45° B=0.00 败，规则给"边"，表记"死"）。28 格需人工判断（LOW_COVERAGE）。
真实阳性锚复导出成立：Pnet_MW 的 A3（域宽比值摆 7.24×＋域共振判定）与 A4（±T/8 −14.1%、无稳健版）为死；deep_dc_rms 的 BH93 静止线 7 对平底读数恒 0 → 配对 0/7 → 死。

## 死在哪一科（ABM_GOODHART_MAP.json）
13 个死格：优化诱发科 6（极值 1、对抗 3、回归·搜索冠军迁移 2），构念缺陷科 7（域共振、相位敏感、BH93 静止态、分母发散、名义效度）。
**两个空列**：因果型（缩短积分）、回归型·排得准的尺子跟踪伪流幅度 D——九项无一有实证检出；A5 行全空。

## 滤链：配置集越大，幸存者越"反"（ABM_FILTER_CHAIN.json、ABM_DERIVED.json）
15344（非退化）→ 5514（＋纬向换平底）→ 5035（＋转风向）→ 4916（＋荒谬黏性）→ 4624（＋陡度轴）。
幸存者中把好坏排反（A ≤ −0.7）的占比 31% → 72%（L3）；两关门 204 → 56，严格门 12 → 2。
与 Skalse 2022 的关系只说"结构同型"：有限配置集确有非退化幸存者，但它们多数不是好尺子。能签的只有带适用域、带 kill 条件的证。

## 预注册记分
11 条真预测 8 中 3 被推翻（P2f 截断作弊会漏网、P2g 区域错置会漏网、P4a L3≤10%），原样保留（verdict.json）。
近邻先亮：Li & Hai arXiv:2607.16646（证伪电池＋变异分析＋盲区点名，2026 同体裁）。本条未另做新颖性检索，不作任何"首次"表述。
